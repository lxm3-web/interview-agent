# interview-agent — 人物專訪製作助理（Agent 版）

claude.ai/code 選這個 repo（或本機 `claude`）→ 說「記者的指示在 inbox，備一場」→ 中英訪綱、給公關的英文信、現場流程、稿件大綱寫到 `outbox/` → 看完說「好，發下去」。

品牌、人物、數據全部虛構。**demo 完歸零**：`inbox/*.done` 改回 `.txt`、清 `outbox/`、log 只留表頭。
